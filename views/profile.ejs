<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect with <%= data.username || 'User' %></title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500&family=Open+Sans&display=swap" rel="stylesheet" />

  <style>
    body {
      margin: 0;
      background: linear-gradient(135deg, #667eea, #764ba2);
      font-family: 'Open Sans', sans-serif;
      color: #222;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
      overflow-x: hidden;
    }

    .container {
      background: white;
      max-width: 420px;
      width: 100%;
      border-radius: 15px;
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
      padding: 30px 25px 40px;
      text-align: center;
      animation: fadeIn 1s ease forwards;
      position: relative;
    }

    .logo {
      width: 100px;
      margin: 0 auto 20px;
      display: block;
      filter: drop-shadow(0 2px 4px rgba(0,0,0,0.15));
    }

    h1 {
      font-family: 'Montserrat', sans-serif;
      font-weight: 600;
      margin-bottom: 25px;
      color: #4a4a4a;
      letter-spacing: 1.1px;
    }

    .icons {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 10px;
    }

    .icons a {
      display: inline-block;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: #f0f0f0;
      box-shadow: 0 3px 6px rgba(0,0,0,0.1);
      transition: background 0.3s ease, transform 0.3s ease;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .icons a:hover {
      background: #764ba2;
      transform: scale(1.1);
      box-shadow: 0 6px 15px rgba(118, 75, 162, 0.6);
    }

    .icons img {
      width: 32px;
      height: 32px;
      filter: grayscale(0.5);
      transition: filter 0.3s ease;
    }

    .icons a:hover img {
      filter: none;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px);}
      to { opacity: 1; transform: translateY(0);}
    }

    @media (max-width: 480px) {
      .container {
        padding: 25px 20px 35px;
      }

      .icons a {
        width: 50px;
        height: 50px;
      }

      .icons img {
        width: 28px;
        height: 28px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Logo from web, swap with your own -->
    <img src="https://cdn-icons-png.flaticon.com/512/747/747376.png" alt="Connect Logo" class="logo" />

    <h1>Connect with <%= data.username || 'User' %></h1>

    <div class="icons">
      <% if (data.facebook) { %>
        <a href="<%= data.facebook %>" target="_blank" rel="noopener" aria-label="Facebook">
          <img src="https://cdn-icons-png.flaticon.com/512/733/733547.png" alt="Facebook" />
        </a>
      <% } %>
      <% if (data.instagram) { %>
        <a href="<%= data.instagram %>" target="_blank" rel="noopener" aria-label="Instagram">
          <img src="https://cdn-icons-png.flaticon.com/512/733/733558.png" alt="Instagram" />
        </a>
      <% } %>
      <% if (data.twitter) { %>
        <a href="<%= data.twitter %>" target="_blank" rel="noopener" aria-label="Twitter">
          <img src="https://cdn-icons-png.flaticon.com/512/733/733579.png" alt="Twitter" />
        </a>
      <% } %>
      <% if (data.youtube) { %>
        <a href="<%= data.youtube %>" target="_blank" rel="noopener" aria-label="YouTube">
          <img src="https://cdn-icons-png.flaticon.com/512/1384/1384060.png" alt="YouTube" />
        </a>
      <% } %>
      <% if (data.tiktok) { %>
        <a href="<%= data.tiktok %>" target="_blank" rel="noopener" aria-label="TikTok">
          <img src="https://cdn-icons-png.flaticon.com/512/3046/3046123.png" alt="TikTok" />
        </a>
      <% } %>
      <% if (data.snapchat) { %>
        <a href="<%= data.snapchat %>" target="_blank" rel="noopener" aria-label="Snapchat">
          <img src="https://cdn-icons-png.flaticon.com/512/185/185966.png" alt="Snapchat" />
        </a>
      <% } %>
      <% if (data.threads) { %>
        <a href="<%= data.threads %>" target="_blank" rel="noopener" aria-label="Threads">
          <img src="https://cdn-icons-png.flaticon.com/512/12105/12105337.png" alt="Threads" />
        </a>
      <% } %>
    </div>
  </div>
</body>
</html>
